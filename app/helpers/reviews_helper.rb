module ReviewsHelper

    # def display_reviews
    #     if @user.reviews.empty?
    #       tag.h2(link_to('No reviews yet - write a review here', new_review_path))
    #     else
    #       user = @user == current_user ? 'Your' : "#{@user.username}'s"
    #       content_tag(:h2, "#{user} #{pluralize(@user.reviews.count, 'Post')}:")
    #     end
    # end

end
